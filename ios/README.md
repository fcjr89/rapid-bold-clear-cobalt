# iOS (Capacitor) — THE CULTURE WAR

Generate on **macOS**:

```bash
npm install
npm run mobile:prepare
npx cap add ios
npm run mobile:ios:sync
npm run mobile:ios
```

Bundle ID: `com.fcjr89.theculturewar`. See `docs/MOBILE.md`.
