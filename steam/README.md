# Steam lead track

Desktop Chromium shell under electron/. Run game dev server, then steam:dev.
F11 fullscreen. Dist scripts build Windows/Linux/macOS installers to steam/dist.
Gamepad mapping in src/game/input.ts. AppID example in steam/.

Install desktop shell packages as devDependencies, then use package.json steam scripts.
Partner: set AppID file from steam_appid.txt.example.
